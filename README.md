# Fitness Tracker App

A React fitness tracker website with registration, login, workout plans, progress tracking, and contact support.

## Available Scripts

- `npm install`
- `npm start`
- `npm run build`
- `npm run deploy`

## GitHub Pages Deploy

This project uses `gh-pages`. Set the repository as your GitHub remote and run `npm run deploy` to publish the built app.
